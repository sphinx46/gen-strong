spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres.railway.internal}:${DB_PORT:5432}/${DB_NAME:railway}?sslmode=require
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:VylKAXPfuMfMUNevYPZtgTDMPXKnvnPQ}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 2
      minimum-idle: 1
      idle-timeout: 60000
      max-lifetime: 300000
      auto-commit: false

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: ${SPRING_FLYWAY_ENABLED:true}
    locations: classpath:db/migrations
    baseline-on-migrate: ${FLYWAY_BASELINE_ON_MIGRATE:true}
    validate-on-migrate: false

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN:8375214694:AAFHRwB2TTT_yn5X1ccLatg1K_sfsyRPjCA}
    username: ${TELEGRAM_BOT_USERNAME:gen_strong_bot}
    name: ${TELEGRAM_BOT_NAME:Gen Strong Bot}
    long-polling:
      timeout: 10
      limit: 20
      max-threads: 2

app:
  security:
    admin-telegram-ids: ${ADMIN_TELEGRAM_IDS:2056410944}

server:
  port: ${PORT:8080}